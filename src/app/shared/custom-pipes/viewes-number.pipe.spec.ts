import { ViewesNumberPipe } from './viewes-number.pipe';

describe('ViewesNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new ViewesNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
