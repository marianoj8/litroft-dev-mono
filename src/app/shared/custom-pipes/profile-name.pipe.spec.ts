import { ProfileNamePipe } from './profile-name.pipe';

describe('ProfileNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ProfileNamePipe();
    expect(pipe).toBeTruthy();
  });
});
